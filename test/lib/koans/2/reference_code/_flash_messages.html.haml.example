- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div#flash{class: "alert alert-#{name.to_s == 'notice' ? 'success' : 'alert'} alert-dismissable", "data-alert" => ""}
      %h5.text-center= content_tag :div, msg
